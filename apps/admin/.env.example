# Drizzle Kit (d1-http driver) — required for db:generate and db:push
# Point to staging for dev, production only when ready to deploy
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_DATABASE_ID=5ddc8080-3ea9-45cc-8fd5-3e59370fce37
CLOUDFLARE_API_TOKEN=

# Wrangler deploy — reads token from env (same token, scoped to account)
# CLOUDFLARE_API_TOKEN (same as above)
